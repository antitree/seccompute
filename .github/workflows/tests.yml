# Superseded by ci.yml — kept as a no-op so existing branch protection
# rules that reference "Tests / test" don't break until they're updated.
name: Tests (legacy)
on:
  pull_request:
  push:
    branches: [main]
jobs:
  redirect:
    name: "See ci.yml"
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is superseded by ci.yml"
